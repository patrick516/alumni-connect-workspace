# Backend origin (no trailing slash). REST calls go to {VITE_API_BASE_URL}/api/...
# Socket.IO uses the same host (path /socket.io).
VITE_API_BASE_URL=http://localhost:5000

# Leave unset during Vite dev to use relative /api + /socket.io proxies instead.
